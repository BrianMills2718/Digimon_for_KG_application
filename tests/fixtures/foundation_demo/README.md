# Synthetic Foundation demo fixture

This is manufactured compatibility/regression data, not an onto-canon production export or real source evidence. The producer field selects the onto-canon6 wire contract; it does not attest that the exporter generated this fixture. It contains one binary assertion with a literal role filler and one ternary assertion, each with a source-window companion.
